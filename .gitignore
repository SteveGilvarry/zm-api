# Rust
/target/
Cargo.lock

# IDE / Tools
.idea/
.vscode/
.VSCodeCounter
*.swp
*.swo
.claude/
.playwright/

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
settings/dev.toml
settings/test.toml
logs/
.env
static/key/*.pem

# Database
zm_schema.sql
*.sql
# ...but the ZoneMinder source-split schema files must be tracked: the test-db
# setup scripts resolve `source db/*.sql` directives from these.
!db/*.sql
# frozen legacy update chain embedded by the upgrade bridge
!db/legacy/*.sql
.grepai/

# Generated by the docs workflow; never committed so it cannot go stale.
book/src/openapi.json
