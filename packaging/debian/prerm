#!/bin/sh
# Debian prerm for zm-api.
set -e

case "$1" in
  remove|deconfigure)
    if [ -d /run/systemd/system ]; then
      systemctl stop zm-api.service || true
      systemctl disable zm-api.service || true
    fi
    ;;
  upgrade|failed-upgrade)
    ;;
esac

exit 0
