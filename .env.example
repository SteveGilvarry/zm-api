APP_PROFILE=dev
RUST_BACKTRACE=1
RUST_LOG=info
# ZoneMinder DB URL for the SeaORM CLI (entity generation, migrations).
# Copy to .env and point at your own database. .env is gitignored — never
# commit real hosts/credentials.
DATABASE_URL=mysql://zmuser:zmpass@localhost/zm
